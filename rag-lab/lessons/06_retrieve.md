# Lesson 6 — Retrieve with VECTOR_SEARCH

Embed the question, then find nearest chunks in BigQuery.

```bash
python -m src.retrieve "How do I reset my password?"
```

Lower cosine `distance` ≈ more similar.
